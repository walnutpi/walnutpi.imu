[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "walnutpi-imu"
version = "0.1.0"
description = "WalnutPi IMU library"
requires-python = ">=3.8"

[tool.setuptools.packages.find]
include = ["walnutpi_imu*"]
